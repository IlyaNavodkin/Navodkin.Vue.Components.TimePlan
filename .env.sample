# Container settings
APP_PORT=8080

# Frontend build-time variables (Vite reads only VITE_*):
# This value is embedded into the built static bundle during docker build.
VITE_API_BASE_URL=https://api.example.com
